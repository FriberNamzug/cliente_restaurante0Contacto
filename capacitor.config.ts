import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'restaurante0Contacto',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
